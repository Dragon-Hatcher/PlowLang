package parser.nodes

/**
 * A combination of one or more explicit values, constants, variables, operators and functions that are interpreted and
 * computed to produce another value.
 */
interface ExpressionParseNode: FunctionReferenceParseNode